# Local development:
NEXT_PUBLIC_API_BASE_URL="http://localhost:4000/api"

# Optional. Leave empty unless Hotjar is approved for the current environment.
# The integration emits event names only and avoids loading on admin/account routes.
NEXT_PUBLIC_HOTJAR_ID=""

# Production behind Caddy on the same domain:
# NEXT_PUBLIC_API_BASE_URL="/api"
